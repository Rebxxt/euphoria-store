import { AuthButtonDirective } from './auth-button.directive';

describe('AuthButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthButtonDirective();
    expect(directive).toBeTruthy();
  });
});
